# DEPRECATED -- Absorbed into /learning-capture

This skill's quality gate logic has been absorbed into `/learning-capture` as an internal sub-step.

**What changed:** Every signal now gets a tier rating (S/A/B/C/D) before being written to disk. Only B tier and above are written. C/D tier learnings are reported as "filtered out" so Eric can override.

The tier criteria (idea count + theme relevance scoring) are now embedded directly in `/learning-capture` Step 1.

For standalone content rating (outside the learning loop), use `/label-and-rate` instead.

Absorbed: 2026-03-28
