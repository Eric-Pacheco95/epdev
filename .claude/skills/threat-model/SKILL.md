# DEPRECATED -- Merged into /red-team --stride

This skill has been merged into `/red-team` with the `--stride` flag.

**Use instead:** `/red-team --stride <system or plan description>`

The `--stride` flag activates full STRIDE threat modeling (Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege) alongside the standard red-team adversarial analysis.

Merged: 2026-03-28
