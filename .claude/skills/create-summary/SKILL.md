# DEPRECATED -- Merged into /extract-wisdom --summary

This skill has been merged into `/extract-wisdom` with the `--summary` flag.

**Use instead:** `/extract-wisdom --summary <content>`

The `--summary` flag produces concise summarization output (TL;DR, KEY POINTS, FACTS AND DETAILS, CONCLUSIONS AND OPEN QUESTIONS) instead of the full wisdom extraction sections.

Merged: 2026-03-28
