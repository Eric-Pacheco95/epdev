# Security Audit Surface

> Auto-generated by `tools/scripts/audit_security_surface.py` on 2026-04-09.
> Source: `security/validators/validate_tool_use.py`
> **Do not edit manually** — re-run the script to refresh.

## Summary

| Category | Count |
|----------|-------|
| Guard functions | 13 |
| Blocked pattern constants | 4 |
| Autonomous-only guards | 5 |
| Protected path patterns | 7 |

## Guard Functions

These functions are called by `validate_bash_command()` or the tool-level validator.

### `_blocked_git_destructive` (line 70)

Patterns from dcg: git commands that destroy uncommitted work.

### `_inline_script_destructive` (line 106)

Patterns from dcg: destructive commands hidden in inline scripts.

### `_blocked_rm_rf` (line 129)

### `_blocked_git_force_main` (line 159)

### `_system_paths_write` (line 169)

### `_remote_pipe_shell` (line 177)

### `_protected_path` (line 183)

Constitutional protected paths and globs.

### `_bash_writes_telos` (line 205)

Check if a Bash command writes to memory/work/telos/ paths.

### `_check_autonomous_telos_write` (line 332)

Block Write/Edit to protected paths in autonomous sessions.

Protected paths: TELOS, context_profiles, research_topics, producers,
settings.json, CLAUDE.md.

IMPORTANT: Any new autonomous entry point (script that calls claude -p or
spawns a worker) MUST set JARVIS_SESSION_TYPE=autonomous in the subprocess
env. Without it, all protections in this function are bypassed (fail-open).
See jarvis_dispatcher.py line 963 for the assertion pattern to copy.

Returns a block result if the write should be blocked, None otherwise.

### `_check_autonomous_git_push` (line 408)

Block ALL git push commands in autonomous sessions (not just force-push).

### `_check_autonomous_read_secrets` (line 415)

Block Read tool on secret files in autonomous sessions.

Interactive sessions rely on user judgment; autonomous sessions must be
validator-enforced because there is no human in the loop.

### `_check_autonomous_file_containment` (line 438)

Block Read/Write/Edit outside worktree in autonomous sessions.

Workers must only access files within their git worktree. The worktree
path is communicated via JARVIS_WORKTREE_ROOT env var (set by dispatcher).
If the env var is not set, this check is skipped (allows non-dispatcher
autonomous jobs like overnight runner to work).

### `_check_overnight_path_scope` (line 477)

Enforce dimension-scoped write rules for overnight autonomous sessions.

Active when JARVIS_OVERNIGHT_DIMENSION is set AND session type is autonomous.
Delegates to overnight_path_guard.validate_write_path() which encodes per-
dimension allowed directories and global BLOCKED_PATHS (including history/).

Returns a block result if the write should be blocked, None otherwise.

## Blocked Pattern Constants

### `INJECTION_SUBSTRINGS` (string list)

**Purpose:** Prompt injection / instruction manipulation strings

- `ignore previous`
- `ignore all previous`
- `disregard previous`
- `disregard all previous`
- `you are now`
- `new instructions:`
- `system prompt`
- `developer message`
- `sudo ignore`
- `dan mode`
- `jailbreak`

### `FORK_BOMB_RE` (regex)

**Purpose:** Fork bomb pattern: :() { :|:& };

```
:\s*\(\s*\)\s*\{\s*:\s*\|\s*:\s*&\s*\}\s*;
```

### `DISK_DANGER` (regex)

**Purpose:** Disk format / partition commands (mkfs, dd, fdisk, diskpart)

```
\b(?:mkfs|mkfs\.\w+|dd\s+.*\bif=/dev/|fdisk|cfdisk|parted|diskpart|format\s+[a-z]:)\b
```

### `PATH_TRAVERSAL_SENSITIVE` (regex)

**Purpose:** Path traversal to sensitive directories (etc, passwd, ssh, root)

```
\.\./.*/(etc|passwd|shadow|ssh|root)|\.\.\\\.\.\\.*\\(etc|passwd)
```

## Protected Path Patterns (`_protected_path`)

These regex patterns in `_protected_path()` cause a block when matched in any Bash command:

- `[/\\]\.ssh[/\\]|[~][/\\]\.ssh\b`
- `\.aws[/\\]credentials`
- `(?:^|[\s/\\])\.env(?:[\s\`
- `[/\\][^\s\`
- `[/\\][^\s\`
- `[^\s\`
- `[^\s\`

## Autonomous-Session-Only Guards

These guards fire only when `JARVIS_SESSION_TYPE=autonomous`.
Interactive sessions rely on operator judgment.

### `_check_autonomous_telos_write`

Write/Edit to TELOS, context_profiles, research_topics, producers, settings.json, CLAUDE.md (autonomous only)

### `_check_autonomous_git_push`

ALL git push commands (autonomous only)

### `_check_autonomous_read_secrets`

Read tool on .env, .ssh/, .aws/, .pem, .key, credentials.json (autonomous only)

### `_check_autonomous_file_containment`

Read/Write/Edit outside JARVIS_WORKTREE_ROOT (autonomous only, when worktree root is set)

### `_check_overnight_path_scope`

Write/Edit outside dimension-scoped allowed dirs during overnight runs (JARVIS_OVERNIGHT_DIMENSION)

## Validation Order in `validate_bash_command()`

1. `_bash_writes_telos` — TELOS write via redirect/tee/cp (autonomous only)
2. Fork bomb regex (`FORK_BOMB_RE`)
3. `_blocked_rm_rf` — recursive delete patterns
4. `_blocked_git_destructive` — git reset --hard, checkout --, clean -f, branch -D, restore, commit --amend, show >
5. `_inline_script_destructive` — python/node/bash -c with dangerous calls
6. `_blocked_git_force_main` — git push --force to main/master
7. `--no-verify` hook bypass
8. `DISK_DANGER` — mkfs, dd, fdisk, diskpart
9. `_system_paths_write` — writes to /etc or /boot
10. `_protected_path` — .ssh/, .env, .pem, .key, credentials, secrets
11. `PATH_TRAVERSAL_SENSITIVE` — ../etc/passwd, ../ssh etc.
12. `INJECTION_SUBSTRINGS` — prompt injection strings
13. `_remote_pipe_shell` — curl/wget | bash
14. `line_has_secret` — secret pattern scanner (API keys, tokens)

**Tool-level guards** (run before `validate_bash_command`):

1. `_check_autonomous_telos_write` — Write/Edit to protected paths
2. `_check_overnight_path_scope` — dimension-scoped writes for overnight
3. `_check_autonomous_read_secrets` — Read on secret files
4. `_check_autonomous_file_containment` — file outside worktree

---
*Re-generate: `python tools/scripts/audit_security_surface.py`*
